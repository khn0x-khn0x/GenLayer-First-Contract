WeatherBet
A simple intelligent contract deployed on GenLayer Testnet.

How it works:
update_storage(string): Updates the stored message.

get_storage(): Retrieves the stored message.

Deployed and verified in GenLayer Studio.
